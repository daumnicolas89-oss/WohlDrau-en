import { AppEntry } from "@/components/AppEntry";

/** Die Web-Version der App: unbeworben, aber funktionsfähig – für alte
 *  Home-Bildschirm-Installationen und alle ohne iPhone. */
export default function AppPage() {
  return <AppEntry />;
}
